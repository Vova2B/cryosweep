Task 7: review dispatched (sonnet, review-task7), BASE=1a8705c HEAD=09f7ce5, 20297-byte package.
  The load-bearing question put to it is the one a clean result hides: **"a ladder that is silent
  because it CANNOT SPEAK is the failure mode here, and it would look exactly like a clean
  result."** The implementer measured window_sensitive quiet at all nine temperatures, correcting
  the spec's own guess — so the reviewer is asked to judge whether the code could be quiet for a
  WRONG reason (a criterion that can never fire, a systematically oversized sigma, or rungs
  excluded before they can disagree). Also asked: whether the rung's `unresolved` REUSES the shared
  helper or merely reimplements it inline and happens to agree today (the numbers matching is not
  the point of ruling 3 — not drifting later is); whether `r_h_spread` can ever be a spurious 0.0
  on perfectly straight synthetic data; and whether the floor's insensitivity test really pins its
  irrelevance or passes for an unrelated reason, since the real file never exercises it — a
  constant nothing tests is a constant that will rot.
